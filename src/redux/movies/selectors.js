export const trendingMovies = (state) => state.movies.items;
export const isLoading = (state) => state.movies.isLoading;
export const moviesError = (state) => state.movies.error;
export const movieInfo = (state) => state.movies.movieInfo;
export const totalPages = (state) => state.movies.totalPages;
export const trailerMovie = (state) => state.movies.trailers;
export const foundMovies = (state) => state.movies.foundMovies;
export const castInfo = (state) => state.movies.castInfo;
export const reviewsInfo = (state) => state.movies.reviewsInfo;
export const isSearched = (state) => state.movies.isSearched;
export const favoriteMovieIds = (state) => state.movies.favorites;
export const favoriteMovies = (state) => state.movies.favoriteMovies;
export const isFavoritesLoading = (state) => state.movies.isFavoritesLoading;
